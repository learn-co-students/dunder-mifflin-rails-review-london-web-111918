<h1>All Dogs</h1>

<%= link_to dogs_path(order: true) do %>
  Order by no. of employees
  <% end %>


<ul>
  <% @dogs.each do |dog| %>
    <li> <%= link_to dog.name, dog_path(dog) %>
    </li>
    <% end %>
</ul>
