<h2>All Employees</h2>

<ul>
  <% @employees.each do |employee| %>
    <li>
      <%= link_to "#{employee.first_name} #{employee.last_name} ", employee %>
    </li>
  <% end %>
</ul>
<%= link_to 'Create new employee', new_employee_path(@employee) %><br>
<%= link_to 'See all dogs', dogs_path  %><br>
